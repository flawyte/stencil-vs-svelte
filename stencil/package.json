{
  "name": "stencil",
  "private": true,
  "version": "0.0.1",
  "description": "Stencil App Starter",
  "scripts": {
    "build": "stencil build && cp now.json www/",
    "deploy": "cd www/ && now",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll"
  },
  "dependencies": {
    "@stencil/core": "^1.0.0-alpha.21"
  },
  "license": "MIT",
  "devDependencies": {
    "@stencil/sass": "^0.2.1"
  }
}
